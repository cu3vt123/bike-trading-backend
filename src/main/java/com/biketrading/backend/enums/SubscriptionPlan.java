package com.biketrading.backend.enums;

public enum SubscriptionPlan {
    FREE,       // Mặc định khi mới tạo tài khoản (0 lượt đăng)
    BASIC,      // Gói thường (Ví dụ: 7 tin)
    VIP,        // Gói VIP (Ví dụ: 15 tin)
    INSPECTION  // Gói dịch vụ kiểm định lẻ (Lấy tích xanh)
}