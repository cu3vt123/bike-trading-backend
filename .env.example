# ===========================================
# ShopBike Frontend – API & Environment
# Copy to .env and adjust for your setup
# ===========================================

# Backend API base URL (no trailing slash)
# Local demo backend: http://localhost:8081/api
# Production: https://your-api.example.com/api
VITE_API_BASE_URL=http://localhost:8081/api

# Origin backend cho demo VNPAY (POST /payment/create) — không có /api
VITE_PAYMENT_API_ORIGIN=http://localhost:8081

# Request timeout in ms (optional, default 15000)
# VITE_API_TIMEOUT=15000

# Use mock data instead of real API
# true = mock (no backend needed), false = call real API
VITE_USE_MOCK_API=false

# VNPay UI: mặc định KHÔNG bảo trì (checkout + gói seller dùng luồng VNPAY/QR).
# Chỉ đặt true khi muốn hiện banner bảo trì tạm thời.
# VITE_VNPAY_MAINTENANCE=true
